name: neat_het_env
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - matplotlib
  - pip
  - pip:
      - git+https://github.com/Finebouche/neat-python.git # -e ../../other/neat-python
      - wandb
      - torch
      - torchvision
      - torchaudio