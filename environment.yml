name: neat_het_env
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - matplotlib
  - swig
  - ffmpeg
  - pip
  - pip:
      - git+https://github.com/Finebouche/neat-python.git # -e ../../other/neat-python
      - wandb
      - graphviz
      - torch
      - torchvision
      - torchaudio
      - "gymnasium[all]"==1.0.0
      - imecilabt-gpulab-cli